import { NgxTagsDirective } from './ngx-tags.directive';

describe('NgxTagsDirective', () => {
  it('should create an instance', () => {
    const directive = new NgxTagsDirective();
    expect(directive).toBeTruthy();
  });
});
